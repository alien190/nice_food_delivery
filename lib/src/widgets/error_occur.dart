import 'package:flutter/material.dart';

class ErrorOccur extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Error occur',style: Theme.of(context).textTheme.body2,));
  }
}
